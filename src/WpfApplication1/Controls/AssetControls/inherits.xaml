<UserControl Visibility="Collapsed" x:Class="AssetBuilder.inherits"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" x:Name="inherits1" DataContext="{Binding ElementName=inherits1}">
    <StackPanel>
		<TextBlock Margin="10,2,10,0" Text="Inherit change" HorizontalAlignment="Center" />
		<ListView Padding="10,2,10,0" Margin="10,2,10,0" Grid.Column="1" ItemsSource="{Binding List}" x:Name="listPanel">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
					<CheckBox IsChecked="False" Content="{Binding}" Checked="CheckBox_CheckedChanged" Unchecked="CheckBox_CheckedChanged" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
	</StackPanel>
</UserControl>
